services:
  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres-pgvector
    image: lawsaw-postgres-pgvector:local
    user: "postgres"
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=128MB"
      - "-c"
      - "work_mem=4MB"
      - "-c"
      - "effective_cache_size=256MB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "wal_buffers=4MB"
      - "-c"
      - "max_wal_size=1GB"
    environment:
      POSTGRES_DB: law_eye
      POSTGRES_USER: law_eye
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      ENABLE_PG_TDE: "1"
      # Ensure SCRAM for both local/host connections (enterprise-grade defaults).
      POSTGRES_INITDB_ARGS: "--auth=scram-sha-256 --auth-host=scram-sha-256 --auth-local=scram-sha-256"
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5435}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      postgres-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U law_eye"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend
    restart: unless-stopped

  postgres-init:
    image: lawsaw-postgres-pgvector:local
    user: "0:0"
    entrypoint: ["bash", "-lc"]
    command: ["chown postgres:postgres /var/lib/postgresql/data"]
    volumes:
      - postgres_data:/var/lib/postgresql/data
    network_mode: "none"
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
    security_opt:
      - no-new-privileges:true
    restart: "no"

  redis-init:
    image: alpine:3.20@sha256:a4f4213abb84c497377b8544c81b3564f313746700372ec4fe84653e4fb03805
    user: "0:0"
    command: ["sh", "-c", "mkdir -p /data/appendonlydir && chown 1000:1000 /data /data/appendonlydir"]
    volumes:
      - redis_data:/data
    network_mode: "none"
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
    security_opt:
      - no-new-privileges:true
    restart: "no"

  redis:
    build:
      context: .
      dockerfile: Dockerfile.redis
    image: lawsaw-redis:local
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    ports:
      - "127.0.0.1:${REDIS_HOST_PORT:-6380}:6379"
    volumes:
      - redis_data:/data
    depends_on:
      redis-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    networks:
      - backend
    restart: unless-stopped

  minio-init:
    image: alpine:3.20@sha256:a4f4213abb84c497377b8544c81b3564f313746700372ec4fe84653e4fb03805
    user: "0:0"
    command: ["sh", "-c", "chown 1000:1000 /data"]
    volumes:
      - minio_data:/data
    network_mode: "none"
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
    security_opt:
      - no-new-privileges:true
    restart: "no"

  minio:
    image: ${MINIO_IMAGE:-alpine/minio:RELEASE.2025-10-15T17-29-55Z@sha256:cf23643a6cf9ce159c57643ceb88279e431262282428c9e0bf3a7ef1a97e84b4}
    user: "1000:1000"
    command: server /data --console-address ":9001"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
    ports:
      - "127.0.0.1:${MINIO_API_PORT:-9000}:9000"
      - "127.0.0.1:${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    depends_on:
      minio-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/minio/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
    networks:
      - backend
    restart: unless-stopped

  rss-fixture:
    # Avoid pulling from DockerHub in restricted environments by reusing the local web runtime image.
    image: lawsaw-web
    profiles: ["e2e"]
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    command:
      [
        "node",
        "-e",
        "const http=require('http'); const fs=require('fs'); const path=require('path'); const file=path.join('/srv','rss.xml'); const server=http.createServer((req,res)=>{ if(req.url!=='/rss.xml'){res.statusCode=404; return res.end('not found');} try{ const data=fs.readFileSync(file); res.setHeader('Content-Type','application/rss+xml; charset=utf-8'); res.end(data);} catch(e){res.statusCode=500; res.end(String(e));}}); server.listen(8000,'0.0.0.0',()=>console.log('rss-fixture listening on :8000/rss.xml'));",
      ]
    volumes:
      - ./apps/web/e2e/fixtures:/srv:ro
    depends_on:
      web:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8000/rss.xml"]
      interval: 5s
      timeout: 3s
      retries: 10
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
    networks:
      - frontend
      - backend
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      # 使用 LAW_EYE__ 前缀匹配 config.rs 中的环境变量解析
      LAW_EYE__SERVER__HOST: "0.0.0.0"
      LAW_EYE__SERVER__PORT: "3001"
      # CSRF allowlist: must include the browser-facing origin (web's host port).
      LAW_EYE__SERVER__ALLOWED_ORIGINS: "${LAW_EYE__SERVER__ALLOWED_ORIGINS:-http://127.0.0.1:18849,http://localhost:18849,http://127.0.0.1:8849,http://localhost:8849}"
      # Local/e2e: allow localhost + docker service URLs for RSS fixtures.
      LAW_EYE__SECURITY__ALLOW_INTERNAL_SOURCE_URLS: "true"
      LAW_EYE__DATABASE__URL: postgres://law_eye:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/law_eye
      LAW_EYE__DATABASE__MAX_CONNECTIONS: "10"
      LAW_EYE__DATABASE__SESSION_ROLE: "law_eye_app"
      LAW_EYE__REDIS__URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      LAW_EYE__AI__API_KEY: ${OPENAI_API_KEY:-}
      LAW_EYE__AI__BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      LAW_EYE__AI__MODEL: gpt-4o-mini
      LAW_EYE__AI__EMBEDDING_MODEL: text-embedding-3-small
      LAW_EYE__OBJECT_STORAGE__ENABLED: "true"
      LAW_EYE__OBJECT_STORAGE__ENDPOINT: "http://minio:9000"
      LAW_EYE__OBJECT_STORAGE__REGION: "us-east-1"
      LAW_EYE__OBJECT_STORAGE__BUCKET: "law-eye"
      LAW_EYE__OBJECT_STORAGE__ACCESS_KEY_ID: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      LAW_EYE__OBJECT_STORAGE__SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      LAW_EYE__OBJECT_STORAGE__FORCE_PATH_STYLE: "true"
      LAW_EYE__OBJECT_STORAGE__SSE_ENABLED: "${LAW_EYE__OBJECT_STORAGE__SSE_ENABLED:-false}"
      WEB_PUSH_VAPID_PUBLIC_KEY: "${WEB_PUSH_VAPID_PUBLIC_KEY:-}"
      WEB_PUSH_VAPID_PRIVATE_KEY: "${WEB_PUSH_VAPID_PRIVATE_KEY:-}"
      WEB_PUSH_SUBJECT: "${WEB_PUSH_SUBJECT:-mailto:admin@lawsaw.io}"
      PRODUCTION: "${PRODUCTION:-false}"
      LAW_EYE__ENCRYPTION__FEEDBACKS__REQUIRE_IN_PRODUCTION: "${LAW_EYE__ENCRYPTION__FEEDBACKS__REQUIRE_IN_PRODUCTION:-true}"
      LAW_EYE__METRICS__TOKEN: "${LAW_EYE__METRICS__TOKEN:-}"
      RUST_LOG: info
    ports:
      - "127.0.0.1:${API_HOST_PORT:-3001}:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/app/law-eye-api", "--healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M
    networks:
      - frontend
      - backend
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      LAW_EYE__SERVER__HOST: "0.0.0.0"
      LAW_EYE__SERVER__PORT: "3002"
      LAW_EYE__SECURITY__ALLOW_INTERNAL_SOURCE_URLS: "true"
      LAW_EYE__DATABASE__URL: postgres://law_eye:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/law_eye
      LAW_EYE__DATABASE__MAX_CONNECTIONS: "10"
      LAW_EYE__DATABASE__SESSION_ROLE: "law_eye_app"
      LAW_EYE__REDIS__URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      LAW_EYE__AI__API_KEY: ${OPENAI_API_KEY:-}
      LAW_EYE__AI__BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      LAW_EYE__AI__MODEL: gpt-4o-mini
      LAW_EYE__AI__EMBEDDING_MODEL: text-embedding-3-small
      LAW_EYE__OBJECT_STORAGE__ENABLED: "true"
      LAW_EYE__OBJECT_STORAGE__ENDPOINT: "http://minio:9000"
      LAW_EYE__OBJECT_STORAGE__REGION: "us-east-1"
      LAW_EYE__OBJECT_STORAGE__BUCKET: "law-eye"
      LAW_EYE__OBJECT_STORAGE__ACCESS_KEY_ID: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      LAW_EYE__OBJECT_STORAGE__SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      LAW_EYE__OBJECT_STORAGE__FORCE_PATH_STYLE: "true"
      LAW_EYE__OBJECT_STORAGE__SSE_ENABLED: "${LAW_EYE__OBJECT_STORAGE__SSE_ENABLED:-false}"
      # Browserless dynamic rendering (when crawler profile is active)
      LAW_EYE__BROWSERLESS__URL: "http://browserless:3000"
      LAW_EYE__BROWSERLESS__TIMEOUT_MS: "30000"
      PRODUCTION: "${PRODUCTION:-false}"
      LAW_EYE__PUSH__PREVIEW_LIMIT: "${LAW_EYE__PUSH__PREVIEW_LIMIT:-3}"
      RUST_LOG: info
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      api:
        condition: service_healthy
      minio:
        condition: service_healthy
      browserless:
        condition: service_healthy
        required: false
    healthcheck:
      test: ["CMD", "/app/law-eye-worker", "--healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    read_only: true
    tmpfs:
      - /tmp
      - /app/.next/cache
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      LAW_EYE_API_PROXY_TARGET: http://api:3001
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "127.0.0.1:${WEB_HOST_PORT:-8849}:8849"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8849/login',{cache:'no-store'}).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    networks:
      - frontend
      - backend
    restart: unless-stopped

  n8n:
    image: ${N8N_IMAGE:-docker.n8n.io/n8nio/n8n:2.4.7@sha256:b9c6ff711128fe2c422fb51ada978040ba3cc5c1fe8934af2d3f4cc5dc47069d}
    # Optional automation service. Keep behind a profile so `docker compose up`
    # remains usable in restricted networks where docker.n8n.io may be unreachable.
    profiles: ["n8n"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
      - LAW_EYE_API_URL=http://api:3001
      - EMAIL_FROM=${EMAIL_FROM:-noreply@laweye.com}
      - EMAIL_TO=${EMAIL_TO:-}
    ports:
      - "127.0.0.1:${N8N_HOST_PORT:-5678}:5678"
    volumes:
      - n8n_data:/home/node/.n8n
      - ./n8n/workflows:/home/node/workflows:ro
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:5678/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      api:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
    networks:
      - frontend
      - backend
    restart: unless-stopped

  browserless:
    image: ${BROWSERLESS_IMAGE:-ghcr.io/browserless/chromium:v2.24.2@sha256:16444c486f709844d6b9a472352a20f0dee924e5421e38a096a7f521b6bec560}
    profiles: ["crawler", "worker", "report"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    environment:
      MAX_CONCURRENT_SESSIONS: ${BROWSERLESS_MAX_CONCURRENT:-5}
      CONNECTION_TIMEOUT: ${BROWSERLESS_CONNECTION_TIMEOUT:-60000}
      PREBOOT_CHROME: "true"
      TOKEN: ${BROWSERLESS_TOKEN:-}
    ports:
      - "127.0.0.1:${BROWSERLESS_HOST_PORT:-3003}:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/json/version"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend
    restart: unless-stopped

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true

volumes:
  postgres_data:
  redis_data:
  n8n_data:
  minio_data:
